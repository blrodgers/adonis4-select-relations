<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Select With Relations</title>
  {{ css('style') }}
</head>
<body>
  <section>
    <div class="logo"></div>
    <div class="title">
    @if(worked)
       Initialized Post and Tag Data
    </div>

    <div class="line"><span> Go See the <a href="{{APP_URL}}/posts">Posts</a> Page now</span></div>

    @else
       Problems Initializing - maybe see Init Controller?
    </div>

    <div class="line"><span> Or you could try the <a href="{{APP_URL}}/posts">Posts</a> Page anyway</span></div>
    @endif

  </section>
</body>
</html>
